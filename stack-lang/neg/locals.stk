fun foo(n) =
  var a = 3;

  if n 5 > then
    val b = n 2 *;
  else
    val c = n 10 +;
    b p                // error
  end

  c p                  // error

  while a 0 > do
    var d = a 2 +;
    d p
    b p                // error
  end

  d p                  // error

  4;

fun bar() = a;        // error

a p                   // error
10 foo p
