namespace jo.regex

import jo.*

section Engine
  type Pattern
  private[regex] defer def compilePattern(source: String, flags: String): Pattern
  private[regex] defer def execPatternAt(pat: Pattern, input: String, pos: Int): Option[MatchResult]
end
