namespace native.GC

// Abstract GC interface --- to be wired by the linker with a concrete
// implementation

// Init GC -- called from Core.init
private[native] defer def init(): Unit receives none

private[native] defer def alloc(size: Int): native.Addr receives none
