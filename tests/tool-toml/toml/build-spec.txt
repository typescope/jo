module:
  app:
    kind = "app"
    links:
      from = "MyApp.Iface.run"
      to = "MyApp.Impl.run"
    modules:
      id = "utils"
      path = "../utils"
    packages:
      name = "http"
      version = "1.0"
    platform = "python"
    src = ["src/", "vendor/"]
  test:
    kind = "app"
    modules = ["app", {id = "testlib", path = "../testlib"}]
    platform = "python"
    src = ["tests/"]
namespace = "MyApp"
platform = "pure"
version = "1.0.0"
