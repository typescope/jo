jo = ">=1.0.0"
name = "my-app"
build = app
main:
  src = ["src/", "vendor/"]
  target = "python"
  dependencies:
    agent-runtime = path "../runtime" [link]
    http = registry "^1.0.0"
    utils = path "../utils"
  links:
    "MyApp.Iface.run" = "MyApp.Impl.run"
test:
  src = ["tests/"]
  dependencies:
    testlib = path "../testlib"
