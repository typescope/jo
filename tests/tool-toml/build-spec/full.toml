jo = ">=1.0"
name = "my-app"

[main]
src = ["src/", "vendor/"]
target = "python"

[main.dependencies]
utils = { path = "../utils" }
http = "^1.0"
agent-runtime = { path = "../runtime", link = true }

[main.links]
"MyApp.Iface.run" = "MyApp.Impl.run"

[test]
src = ["tests/"]

[test.dependencies]
testlib = { path = "../testlib" }
