jo = "1.0"

[module.api]
kind = "lib"
src = ["api/src/"]

packages = [{ name = "jo-core", version = "1.0" }]

[module.api.package]
name = "agent-api"
version = "1.0.0"
description = "Sandbox agent framework API"

[module.test]
kind = "app"
platform = "python"
src = ["test/src/"]

modules = ["api"]
packages = [{ name = "jo-test", version = "0.1" }]
