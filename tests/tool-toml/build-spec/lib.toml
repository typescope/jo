jo = "1.0"

[module.api]
kind = "lib"

dependencies = [
  { package = "jo-core", version = "1.0" },
]

[module.api.package]
name = "agent-api"
version = "1.0.0"
description = "Sandbox agent framework API"
runtime = "pure"

[module.test]
kind = "app"

dependencies = [
  { module = "api" },
  { package = "jo-test", version = "0.1" },
]
