namespace ExternalResources

import jo.resource.*

param resources: Resources

def printResource(label: String): Unit receives IO.stdout, resources =
  match resources.readText("ext-assets/ext.txt")
  case Ok(text) => println(label + " external source resource = " + text.trim)
  case Err(err) => println(label + " external source resource err = " + err)
