jo = "1.0"

[module.app]
kind = "app"
platform = "python"
src = ["src/"]
depth = 3

modules = [{ id = "lib", path = "z-local" }]
packages = [{ name = "alpha-pkg", version = "1.0" }]

[module.test]
kind = "app"
platform = "python"
src = ["tests/*.jo"]

modules = ["app"]
packages = [{ name = "test-only-pkg", version = "1.0" }]
