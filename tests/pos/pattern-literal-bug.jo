type Result = List[Result] | String

def main =
  val res: Result = ["hello", "world"]

  match res
    case List ["hello", ..] => println "matched"
    case _ => println "not match"

  if res is List(["hello", ..]) then
    println "matched"
