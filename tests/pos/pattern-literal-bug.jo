type Result = List[Result] | String

def main =
  val res: Result = ["hello", "world"]

  // match res
  //   case ["hello", ..]: List[Result] => println "matched"
  //   case _ => println "not match"

  if res is (l: List[Result]) && l is ["hello", ..] then
    println "matched"
