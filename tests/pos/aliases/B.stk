namespace B

import A.Message


section Inner
  type Message = String
  data Option[T] = Some(v: T) | None

  alias B.foo
end

alias Inner.Message

param prefix: String = "[info]"
fun foo(s: Message) =
  println (prefix + s)
