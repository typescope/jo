namespace B

import A.Message
import A.Flags

section Inner
  type Message = String
  union Option[T] = Some(v: T) | None

  alias def foo = B.foo
end

section Nested
  def bar(x: Flags) = println x
end

param prefix: String = "[info]"
def foo(s: Message) =
  println (prefix + s)
