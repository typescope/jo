def inspect(l: List[Int]) =
  if l.size == 0 then
    println "empty"
  else
    print "["
    for i in l do print ((intToStr i) + ", ")
    println "]"

class IntArray
  val array: Array[Int]

  def IntArray(size: Int) =
    this.array = Array.create(size)

  def get(i: Int) =
    Array.get array i

  def set(i: Int, value: Int) =
    Array.set array i value
end

def main =
  val l1 = [1, 2, 5, 8]
  val l2 = [4, 9, ..l1, 10]
  println l1[0]
  println l2[1]

  inspect []
  inspect l2
  inspect [100, 200]

  val arr = new IntArray(3)
  arr[0] = 10
  arr[1] = 20
  arr[2] = 30

  inspect [arr[0], arr[1], arr[2]]
