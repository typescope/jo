union Lst[U] = Nil | Cons(head: U, tail: Lst[U])

def Lst[T] = (a: T) => Cons a Nil

def foo(): Int = 30
def bar: Int = 20

def main =
  foo()
  bar()

  // match Lst(4)
  //   case Cons(a, _) => println a
  //   case _ =>

  match Lst 5
    case Cons(a, _) => println a
