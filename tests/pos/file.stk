fun main =
  stdout.write("name: ")
  val name = stdin.readLine with stdin = object { def readLine = "stk" }
  println
    "hello, " .+ name

  val file = open("abc.txt")
  file.write("123456789")
  file.seek 0
  stdout.write
    file.readLine .+ "\n"
