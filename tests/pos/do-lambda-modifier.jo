def main() =
  val xs = [1, 2, 3, 4]

  println:
    xs.exists: x => x > 3

  println:
    xs.forall: x => x > 0

  println:
    xs.count: x => x % 2 == 0

  println:
    xs.fold: 0, (acc, x) => acc + x

  println:
    xs.fold: 0, (acc, x) =>
      if x > 0 then acc + x else acc
