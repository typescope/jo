def effect(n: Int): Int =
  println n
  n

def main =
  def foo: Int =
    if (effect 2) > 0 then
      val c = effect 3
      c
    else
      val c = effect 4
      c

  val r = (effect 1) + foo

  println r
