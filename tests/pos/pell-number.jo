// https://en.wikipedia.org/wiki/Pell_number

def pell(n: Int): Int = pellComp 0 1 0 n

def pellComp(a: Int, b: Int, i: Int, n: Int): Int =
  if i == n then
    a
  else
    println a
    pellComp b (a + (b * 2)) (i + 1) n

def main = println (pell 20)
