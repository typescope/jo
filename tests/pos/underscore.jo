type StringRepr =
    #Raw(str: String)
  | #Concat(length: Int, lhs: StringRepr, rhs: StringRepr)


def length(s: StringRepr) =
  match s
    case #Raw(str) => str.size
    case #Concat(len, _, _) => len

def main =
  val len = length #Raw("hello")
  println len
