data Value = StringValue(s: String) | IntValue(i: Int)

data QueryParts(sql: String, params: List[Value])

def main: Unit =
  val query = QueryParts("Age >", [IntValue(3)])
  query.params.foreach v =>
    match v
      case StringValue s => println s
      case IntValue n => println n
