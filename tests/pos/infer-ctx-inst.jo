union Value = StringValue(s: String) | IntValue(i: Int)

class QueryParts(sql: String, params: List[Value])

def main: Unit =
  val query = QueryParts("Age >", [IntValue(3)])
  for v in query.params do
    match v
      case StringValue s => println s
      case IntValue n => println n
