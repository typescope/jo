class Box[T]
  val value: T

  def Box(v: T) = this.value = v

  def map[S](f: T => S): Box[S] = new Box(f value)

def main =
  val a = new Box(4)
  println (a.map(i => i + 3).value)

  a.value = 5
  println (a.map(i => i * 2).value)
