namespace test

type A = Int

type List[T] = #Nil | #Cons(head: T, tail: List[T])

fun double(n: test.A): A = n + n

fun concat(a: Int, l: test.List[Int]): List[Int] = #Cons(a, l)

fun main =
  test.double 10 p
