// Union def before user section — synthetic still merges INTO user section.

union Option[T] = Some(value: T) | None
  def isEmpty: Bool =
    match this
      case Some(_) => false
      case None => true
    end
  end
end

section Option
  def isEmpty: Bool = false
end
