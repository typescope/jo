pattern Pos: Partial[Int] = case x if x > 0

def test(a: List[Int]) =
  match a
    case [.. while (x if x <= 2), x, ..ys] =>
      println x
      val c = ys

    // case _ =>

def test2(a: List[Int]) =
  match a
    case [.. while (x if x <= 2), ..ys while Pos, ..zs] => pass

  match a
    case [x, ..ys] => pass

  match a
    case [x, ..ys, .. while Pos] => pass
    case _ => pass

  match a
    case [x, ..y, Pos] => pass
    case _ => pass
