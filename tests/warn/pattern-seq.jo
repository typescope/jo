pattern Pos: Partial[Int] = case x if x > 0

def test(a: List[Int]) =
  match a
    case [.. while (x if x <= 2), x, ..ys] =>
      println x
      val c = ys

    // case _ =>

def test2(a: List[Int]) =
  match a
    case [.. while (x if x <= 2), ..ys while Pos, ..zs] =>

  match a
    case [x, ..ys] =>

  match a
    case [x, ..ys, .. while Pos] =>
    case _ =>

  match a
    case [x, ..y, Pos] =>
    case _ =>
