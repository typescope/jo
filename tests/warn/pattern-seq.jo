pattern Pos: Partial[Int] = case x if x > 0

def test(a: List[Int]) =
  match a
    case [> (x if x > 2), _ys*] =>
      println (intToStr x)
      val c = _ys

    // case _ =>

def test2(a: List[Int]) =
  match a
    case [> (x if x > 2), (ys @ Pos)*, z*] =>

  match a
    case [x, y*] =>

  match a
    case [x, y*, Pos*] =>
    case _ =>

  match a
    case [x, ..y, Pos] =>
    case _ =>
