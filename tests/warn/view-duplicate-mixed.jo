// One direct and one delegate view of the same interface are not allowed

interface Logger
  def log(msg: String): Unit
end

class Service(logger: Logger)
  def log(msg: String): Unit = ...
  view Logger
  view Logger = logger
end

def main = ...
