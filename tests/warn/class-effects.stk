param out: String => Unit

fun bar(s: String) = out s

class Foo
  def Foo() receives out =
    out "hello"
    {}

  def foo(x: Int) receives none = bar (intToStr x)

  def work() = bar "work"
end

fun instantiate receives none = new Foo

fun test(o: Foo) receives none = o.work()
