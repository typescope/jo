fun ok(a: ..Int) = a

fun test1(a: ..Int, b: Int) = b

fun test2(l: List[..Int]) = ???

pattern Pat(a: ..Int): List[Int] =
  case a

type A = #Res(x: ..Int)

// type B = Predef...[Int]  // check prefix selection

fun mix(x: Int, l: List[Int]): Unit = ???

fun main =
  val l: ..Int = List.empty

  mix 6 List 8 9

  .. l

  println (.. List("hello"))
