param out: String => Unit

def bar(s: String) = out s

class Foo
  def Foo() receives out =
    out "hello"

  def foo(x: Int) receives none = bar (intToStr x)

  def work() = bar "work"
end

def instantiate receives none = new Foo

def test(o: Foo) receives none = o.work()
