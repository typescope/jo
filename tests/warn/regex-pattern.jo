import jo.regex.*

def testRebinding(s: String) =
  if s is m @ `a` && s is m @ `b` then
    ...

def testWrongType(s: String) =
  if s is m: String && s is m @ `a` then
    ...

def testScrutineeType(n: Int) =
  if n is `\d+` then
    ...
