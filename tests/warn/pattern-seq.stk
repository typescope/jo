type List[T] = object {
  def size: Int
  def apply(i: Int): T
}

pattern Pos: Partial[Int] = case x if x > 0

fun test(a: List[Int]) =
  match a
    case [> (x if x > 2), _ys*] =>
      println (intToStr x)
      List ys foreach (x => println (intToStr x))

    // case _ =>

fun test2(a: List[Int]) =
  match a
    case [> (x if x > 2), (ys @ Pos)*, z*] =>

  match a
    case [x, y*] =>

  match a
    case [x, y*, Pos*] =>
    case _ =>
