def ok(a: ..Int) = a

def test1(a: ..Int, b: Int) = b

def test2(l: List[..Int]) = ???

pattern Pat(a: ..Int): List[Int] =
  case a

type A = #Res(x: ..Int)

// type B = Predef...[Int]  // check prefix selection

def mix(x: Int, l: List[Int]): Unit = ???

def main =
  val l: ..Int = List.empty

  mix 6 List 8 9

  .. l

  println (.. List("hello"))

  ok(3, .. [] 3, 5)

  ok(10, ..)
