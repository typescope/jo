namespace subprocess

private def subprocessModule: py.Value = py.importModule("subprocess")

//[ Run a command without capturing output.   //]
def run(args: py.List): Unit = subprocessModule.run(args)
